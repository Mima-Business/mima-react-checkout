import { MimaButtonProps } from '../types';
export declare const MimaButton: ({ fullWidth, title, className, onSuccess, onClose, ...checkoutProps }: MimaButtonProps) => import("react/jsx-runtime").JSX.Element;
export default MimaButton;
